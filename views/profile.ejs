<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>User Profile Viewer</title>
<link rel="stylesheet" href="stylesheets/profile.css">
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="container">
    <div class="profile-card">
      <div class="profile-header">
        <h1>User Profile</h1>
        <div class="w-full h-full bg-pink-900 rounded-lg p-10 text-white">
  
         
          <form action="/update/<%= user.id%>" method="post">
              <input name="name" type="text" placeholder="Name" class="px-5 py-2 rounded-lg border-2 border-zinc-800 bg-transparent" value="<%= user.name%>">
              <input name="email" type="text" placeholder="Email" class="px-5 py-2 rounded-lg border-2 border-zinc-800 bg-transparent" value="<%= user.email%>">
              <input name="image" type="text" placeholder="Phone Number" class="px-5 py-2 rounded-lg border-2 border-zinc-800 bg-transparent" value="<%= user.phoneNum%>">
              <input type="submit" class="px-5 py-2 bg-pink-500 rounded-lg ml-3 text-black" value="Update User">
          </form>
  
      </div>
      </div>
      <div id="profile" class="profile-content"></div>
    </div>
  </div>
  <script src="profile.js"></script>
</body>
</html>
